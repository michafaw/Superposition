///scrSineWave(amplitude, frequency, phase, time)

var amplitude = argument0;
var frequency = argument1;
var phase = argument2;
var time = argument3;


return amplitude*sin(2*pi*frequency*time + phase);
